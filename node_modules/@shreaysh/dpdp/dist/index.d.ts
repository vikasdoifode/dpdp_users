export interface PermissionData {
    subject: string;
    description: string;
}
export interface DataRecordInput {
    type: 'email' | 'phone' | 'address';
    value: string;
}
export default class DPDPClient {
    private apiKey;
    private baseUrl;
    constructor(apiKey: string, baseUrl: string);
    private request;
    createUser(): Promise<string>;
    storePermission(userUID: string, data: PermissionData): Promise<string>;
    storeData(userUID: string, data: DataRecordInput): Promise<string>;
    revokePermission(userUID: string, id: string): Promise<void>;
    revokeData(userUID: string, id: string): Promise<void>;
    deletePermission(userUID: string, id: string): Promise<void>;
    deleteData(userUID: string, id: string): Promise<void>;
    viewPermission(userUID: string, id: string): Promise<any>;
    viewData(userUID: string, id: string): Promise<any>;
}
